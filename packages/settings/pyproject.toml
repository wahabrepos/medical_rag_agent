[project]
name = "medrag-settings"
version = "0.1.0"
description = "Typed runtime settings shared by all services."
requires-python = ">=3.12,<3.13"
dependencies = [
    "pydantic-settings>=2.10",
]

[build-system]
requires = ["uv_build>=0.8.15,<0.9"]
build-backend = "uv_build"
